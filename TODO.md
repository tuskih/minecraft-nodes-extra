top priority

make warzone system configurable (mainly resources, capture times etc)

optimize whole plugin

make nodes itself a more popular plugin, doing so would require modern plugin support, maybe rewrite the whole code

medium priority

make plugin actually translatable, would genuinely be a pain in the ass to do so

low priority

add new features (features are good enough for now)

make code more readable
